# 프로젝트 개요

이 저장소는 "애플리케이션 개발자를 위한 LLM의 원리"(부제: 동작 원리부터 오픈 모델 표준, RAG까지) 책 프로젝트다 (2026-08-14 시작). GitHub 저장소는 `benelog/llm-principles`(SSH: git@github.com:benelog/llm-principles.git, main 브랜치)다. 기존 `llm-principle`(단수) 저장소를 rename해서 만들었다.

## 원고 구조

- `book/`에 AsciiDoc 원고가 도입 장(ch00, 번호 없는 장)과 4개 부, 13개 장으로 구성된다. ch00은 book.adoc에서 `:sectnums!:`로 감싸 번호 없이 include되므로 "N장" 번호에 포함되지 않는다.
  - 1부 정보 레이어: ch01 모델 레이어, ch02 프롬프트 레이어, ch03 회색지대
  - 2부 microGPT와 동작 원리: ch04, ch05, ch06 추론 서빙의 원리
  - 3부 오픈 모델 표준: ch07 가중치 포맷, ch08 아키텍처 규약, ch09 토크나이저·채팅 템플릿
  - 4부 RAG: ch10 파이프라인, ch11 한국어, ch12 진단, ch13 RAG의 경계
- 본문의 장 상호 참조는 "N장" 텍스트로 쓰므로 장 추가·삭제 시 번호를 일괄 갱신해야 한다.
- 부 구성과 partintro는 `book.adoc`에 있고, 장 파일은 `= 제목`으로 시작해 leveloffset=+1로 include된다.
- `book/book.html`은 gitignore된 로컬 빌드 산출물이고 배포는 GitHub Actions가 한다.
- 블로그 출처 표기는 본문에 넣지 않는다.
- 원고는 사용자의 블로그 글 3편(huggingface-open-model-standards, llm-information-layers, rag-system-components)과 Karpathy의 microGPT 포스트를 바탕으로 한다.

## 예제 코드

- `examples/go/`: microgpt 포팅, serving 추론 비용 측정, safetensors 파서, quantize 양자화 오차 측정, bpe 토크나이저, minirag 하이브리드 검색. 순수 표준 라이브러리만 사용.
- `examples/spring-ai/`: Spring Boot 3.5 + Spring AI 1.0.9. 내장 ONNX 임베딩으로 API 키 없이 실행 가능하고 Ollama는 선택.
- 원고의 코드는 반드시 `include::../examples/...[tag=...,indent=0]` 태그 문법으로 예제 소스를 인용한다(코드 복사 금지).
- 예제 코드에는 `// tag::이름[]` 주석이 있으니 수정 시 태그를 깨뜨리면 안 된다. 수정 후 `cd book && asciidoctor book.adoc`으로 include 해석을 검증한다.

## AsciiDoc 표기 지침

인라인 마크업(`*굵게*`, `_기울임_`, `` `코드` ``)의 기본형은 constrained 문법이라 여는 기호 앞이나 닫는 기호 뒤에 단어 문자가 붙으면 렌더링되지 않는다. Asciidoctor는 한글도 단어 문자로 취급하므로, 한국어 조사가 바로 이어지는 `*벡터*는`, `` `usage`다 ``는 별표와 백틱이 그대로 노출된다. 뒤에 공백이나 괄호가 오는 경우만 정상 동작한다.

- 한글이 바로 이어붙는 자리에서는 기호를 두 번 쓴 unconstrained 문법을 쓴다: `**벡터**는`, `` ``usage``다 ``, `__기울임__은`.
- URL이나 파일명 안의 밑줄은 강조가 아니므로 건드리지 않는다.
- 검증은 `cd book && asciidoctor book.adoc` 후 생성된 HTML 본문에 `*`나 백틱이 남아 있는지 확인한다.

## 한국어 문체 지침

한국어 원고나 문서(README, 주석 포함)를 쓸 때 아래 지침을 따른다. 한국어 산문을 쓴 뒤 아래 표현들을 grep으로 검사하고 대체한다.

### 줄표(—)

- 본문·제목·목차에서 쓰지 않는다. 삽입구는 괄호로("관계형 스키마(외래 키, JOIN)를"), 문장 연결은 두 문장으로 나누고, "제목 — 부제"는 "제목: 부제"로 바꾼다.
- 실제 설정 파일이나 출력을 인용한 문자열과 코드 블록 안 내용은 원문 그대로 둔다.

### 은유 동사

- "무너진다": 무너지는 대상과 결과가 바로 이어서 구체적으로 설명될 때만 쓴다. 그 외에는 실제로 무엇이 어떻게 잘못되는지 쓴다("편집 리듬이 무너진다" → "파일 하나를 고칠 때마다 전체 테스트를 기다리게 된다"). 보안 문맥은 "뚫린다"처럼 실제 사건을 가리키는 동사를 쓴다.
- "맞물린다": 쓰지 않는다. 실제 관계가 드러나게 바꾼다. 인과는 "~일수록 ~에 유리하다", 결합 효과는 "A에 B가 더해져", 근거는 "이유는 ~에 있다", 사용 관계는 "~를 ~가 어떻게 사용하는지".
- "가른다/갈린다": 원칙적으로 쓰지 않는다. 단순 분류는 "나뉜다", 정적인 역할 배정은 "구분한다", 조건 분기는 "정한다"(가능하면 대응 규칙까지 쓴다), 기준에 따라 답이 달라지는 서술은 기준별 결론을 직접 쓴다. "의견이 갈린다" 같은 굳은 관용구는 허용한다.
- "스며 있다/스며든다": "여러 곳에서 쓰인다", "모든 응답에 일관되게 나타난다"처럼 실제 작용으로 바꾼다.
- "흔든다", "숨어 있다" 같은 은유 동사도 실제 변화로 바꾼다("뼈대를 흔드는 변화" → "구성의 전제를 바꾸는 변화").

### 추상 명사·공간 은유

- "가볍다/무겁다": 소프트웨어 묘사에 쓰지 않는다. 메모리("수십 MB 메모리로 도는"), 크기("아티팩트가 크다"), 시간("오래 걸리는"), 절차("준비가 간단하다")처럼 기준을 명시한다.
- "지점": 굳은 기술 용어(조립 지점, 발생 지점)와 본래 의미(프랜차이즈 지점)만 허용한다. 그 외에는 부분, 곳, 대목, "어디서"로 바꾼다.
- "축": 좌표축·도표의 축과 기준 두 개를 직교시키는 진짜 2차원 구도에만 쓴다. 기준·원인·항목을 가리키는 용법은 "기준", "원인", "이 네 가지"로 바꾼다.
- "겹": 실제 층 구조나 물리적 겹침에만 쓴다. 추상 명사에 얹는 용법은 개수("네 가지가 한꺼번에")나 "더해진다"로 바꾼다.
- "손잡이": 물리적 손잡이에만 쓴다. 설정·제어 수단 비유는 무엇을 어떻게 조작하는지 쓴다("환경 변수라는 손잡이" → "환경 변수로 동작 바꾸기").
- "층/층위": 신경망의 층(layer)처럼 실체가 있을 때만 쓴다. 추상적 구분은 실제 차이 기준으로 바꾼다("여러 층위가 있다" → "규모가 다양하다").
- "뼈대": "기본 구조", "전체 구조", "기본 절차"처럼 무엇의 어떤 구조인지 드러나게 바꾼다.
- "틀": 가리키는 대상이 바로 앞뒤에서 구체적으로 정의될 때만 쓴다("강화학습의 틀은 네 가지 요소로 이루어진다"). 지시어로만 받는 "이 틀"은 실제 대상으로 바꾼다("이 틀 안에 있다" → "마르코프 체인으로 볼 수 있다", "이 틀에서 보인다" → "이 대응에서 보인다").
- "지도", "밑바탕" 같은 공간 은유는 실제 하는 일로 바꾼다. 책 전체를 관통하도록 의도한 비유(예: "정보 전달 방법의 전체 지도를 그린다")는 남길 수 있지만, 은유를 겹쳐 쓰지 않는다("그 지도의 밑바탕인" → "그 분류의 바탕인").

### 한정·종결 표현

- "사실상": 굳은 용어("사실상 표준")이거나 정확성에 꼭 필요한 한정일 때만 쓴다. 그 외에는 삭제하거나 풀어쓴다("사실상 0" → "0에 가깝다", "사실상 필수다" → "필수가 된다").
- "~인 셈이다": 비유를 맺을 때만 쓴다. 일반 서술의 결론은 직설 종결("확장된 셈이다" → "확장됐다"). 대체 표현이 "~인 것이다"로 쏠리지 않게 한다.

### 비유의 판단 기준

- 비유는 바로 다음 문장에서 구체적 서술로 풀릴 때만 유지한다(예: "코퍼스의 분포가 곧 모델의 세계관이 된다" 뒤에 "많이 등장한 주제는 잘 알고, 드물게 등장한 주제는 부정확하게 알고"가 이어지는 경우).
- 은유가 두 개 이상 겹친 문장("뼈대를 흔드는", "지도의 밑바탕")은 반드시 풀어 쓴다.
- 기술 용어로 굳은 표현(표면 문자열, 회색지대, RAG의 경계)은 은유 검사 대상이 아니다.
