[[ch-microgpt-basics]]
= 다음 토큰 예측과 자동 미분

== 200줄로 압축한 GPT

microGPT는 Andrej Karpathy가 만든, 의존성 없는 순수 파이썬 200줄로 GPT를 학습하고 추론하는 프로젝트다. PyTorch도, NumPy도 쓰지 않는다. 그런데도 데이터셋, 토크나이저, 자동 미분, 신경망 아키텍처, 옵티마이저, 학습 루프, 추론까지 GPT를 구성하는 필수 요소를 전부 담고 있다.

이 코드가 가치 있는 이유는 규모가 아니라 구조에 있다. ChatGPT 같은 프로덕션 LLM과 microGPT의 차이는 파라미터 수, 데이터 양, 하드웨어에 있지 동작 원리에 있지 않다. 200줄을 이해하면 수천억 파라미터 모델이 무엇을 하는지도 같은 틀로 이해할 수 있다.

이 장과 다음 장에서 microGPT의 구성 요소를 하나씩 해설한다. 코드는 원본 파이썬 대신 이 책의 저장소에 포함된 Go 포팅(`examples/go/microgpt`)에서 인용한다. 언어만 다를 뿐 원본의 구조를 그대로 따랐고, 외부 모듈 없이 `go run .` 으로 실행된다. 이 장은 학습의 기반이 되는 두 부품인 토크나이저와 자동 미분을 다루고, 다음 장에서 모델 아키텍처와 학습, 추론을 다룬다.

== 문제 정의: 다음 토큰 예측

microGPT가 푸는 문제는 단순하다. 수천 개의 영어 이름(emma, olivia, liam, ...)을 보고 그 통계적 패턴을 학습해서, 세상에 없던 그럴듯한 새 이름을 만들어 내는 것이다.

이 단순한 과제가 GPT 계열 모델 전체의 과제와 같은 형태라는 사실이 중요하다. GPT가 하는 일은 언제나 하나다. 지금까지의 토큰 열을 보고 다음 토큰의 확률 분포를 예측한다. 이름 생성기는 "e, m, m 다음에는 a가 올 확률이 높다"를 배우고, ChatGPT는 "질문 다음에는 이런 답변 토큰이 올 확률이 높다"를 배운다. 규모가 다를 뿐 같은 예측 문제다.

"확률 분포를 예측한다"는 표현을 구체적으로 하면 이렇다. 모델의 출력은 어휘에 있는 모든 토큰 각각에 대한 점수(로짓)이고, 이 점수를 softmax 함수로 확률로 바꾼다. "다음 글자는 a일 확률 31%, n일 확률 12%, ..." 같은 분포가 매 위치에서 나온다. 학습은 이 분포가 실제 데이터와 가까워지도록 파라미터를 조정하는 일이고, 추론은 이 분포에서 토큰을 뽑는 일이다.

== 토크나이저: 텍스트를 숫자로

신경망은 숫자만 다룬다. 그래서 텍스트를 정수 토큰의 열로 바꾸는 단계가 필요하다. microGPT의 토크나이저는 가장 단순한 방식인 문자 단위 토크나이저다.

[source,go]
----
include::../examples/go/microgpt/tokenizer.go[tag=tokenizer,indent=0]
----

데이터셋에 나오는 고유 문자(소문자 a부터 z까지 26자)에 정수를 하나씩 할당하고, 특수 토큰 BOS(Beginning of Sequence)를 추가한다. 어휘 크기는 27이다.

BOS는 시퀀스의 시작과 끝을 표시한다. 학습할 때는 각 이름의 앞뒤에 BOS를 붙여서 "이름이 시작된다", "이름이 끝났다"는 신호까지 함께 학습시킨다. 추론할 때는 BOS로 시작해서 모델이 다시 BOS를 뱉으면 이름이 완성된 것으로 본다. 프로덕션 모델의 채팅 템플릿에 있는 `<|im_start|>` 같은 특수 토큰들이 이 BOS의 확장판이다. 대화의 역할 구분, 도구 호출의 경계 표시가 전부 특수 토큰으로 구현된다.

프로덕션 LLM은 문자 단위 대신 BPE(Byte Pair Encoding) 같은 서브워드 방식을 쓴다. 자주 함께 나타나는 문자 조합을 병합해서 하나의 토큰으로 만드는 알고리즘으로, 어휘 크기가 10만 개를 넘는다. 문자 단위보다 시퀀스가 짧아져서 같은 컨텍스트 윈도우에 더 많은 내용이 들어간다. 방식은 달라도 "텍스트를 정수 열로 바꾸고, 특수 토큰으로 구조를 표시한다"는 역할은 같다.

== 자동 미분: 학습을 가능하게 하는 엔진

microGPT에서 가장 교육적인 부분이다. PyTorch의 `autograd`가 하는 일을 스칼라 값 하나 단위로 직접 구현한다.

=== 왜 미분이 필요한가

학습의 목표는 손실(모델 예측이 정답에서 벗어난 정도)을 줄이는 것이다. 파라미터가 수천 개일 때 어느 파라미터를 어느 방향으로 움직여야 손실이 줄어드는지를 알려면, 각 파라미터에 대한 손실의 기울기(gradient)가 필요하다. 기울기는 "이 파라미터가 조금 커지면 손실이 얼마나 변하는가"라는 값이다. 기울기의 반대 방향으로 파라미터를 조금씩 움직이는 것이 경사 하강법이고, 모든 딥러닝 학습의 뼈대다.

문제는 손실이 파라미터로부터 수만 번의 연산을 거쳐 계산된다는 것이다. 이 긴 연산 사슬을 통과하는 기울기를 손으로 유도할 수는 없다. 자동 미분은 연산이 일어날 때마다 계산 과정을 그래프로 기록해 두었다가, 기울기를 기계적으로 역산하는 기법이다.

=== Value: 계산을 기록하는 숫자

핵심은 `Value` 타입이다. 숫자 하나(`Data`)와 그 숫자에 대한 손실의 기울기(`Grad`)를 함께 들고 다니면서, 연산이 일어날 때마다 계산 그래프를 기록한다.

[source,go]
----
include::../examples/go/microgpt/value.go[tag=value,indent=0]
----

덧셈, 곱셈 같은 연산은 결과 `Value`를 만들면서 두 가지를 함께 기록한다. 어떤 값들로부터 만들어졌는지(자식 노드), 그리고 역방향으로 기울기를 전달하는 방법(로컬 미분)이다. 예를 들어 `c = a.Mul(b)`라면 "c의 기울기에 b의 값을 곱한 것이 a로 전달된다"는 규칙이 클로저로 저장된다.

=== Backward: 체인 룰의 기계적 적용

학습의 한 스텝이 끝나면 손실값에서 `Backward()`를 호출한다. 계산 그래프를 위상 정렬한 뒤 역순으로 순회하면서 체인 룰을 적용하면, 손실에서 출발한 기울기가 모든 파라미터까지 전파된다.

[source,go]
----
include::../examples/go/microgpt/value.go[tag=backward,indent=0]
----

이 결과로 각 파라미터의 `Grad` 필드에는 "네가 조금 커지면 손실이 얼마나 변하는지"가 채워진다. 역전파(backpropagation)라고 부르는 이 절차는 어떤 연산 조합에도 똑같이 작동한다. 다음 장에서 만들 어텐션과 MLP가 아무리 얽혀 있어도, 각 연산이 자기 로컬 미분만 기록해 두면 전체 기울기는 자동으로 계산된다. PyTorch와 다른 점은 PyTorch가 스칼라가 아닌 텐서 단위로, GPU에서 이 일을 한다는 것뿐이다.

두 부품이 준비되었다. 텍스트를 숫자로 바꾸는 토크나이저와, 어떤 계산이든 기울기를 역산해 주는 자동 미분이다. 다음 장에서 이 위에 GPT를 세운다.
